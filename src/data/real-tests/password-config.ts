/**
 * TESTS PASSWORD CONFIGURATION
 * 
 * Change the password below to control access to the /tests page.
 * The password is checked client-side and stored in sessionStorage.
 */
export const TESTS_PASSWORD = 'realcswtests';

/**
 * TESTS FEATURE TOGGLE
 * 
 * Set to `true` to enable the /tests section of the site.
 * When `false`, all /tests and /password routes are completely removed
 * and the pages cannot be accessed at all.
 */
export const TESTS_ENABLED = false;
